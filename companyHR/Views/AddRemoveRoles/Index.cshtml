


    
@model List< company.BL.Model.UsersRolesVm>
@{
    ViewBag.Title = "Users Roles";
}

<!-- Exportable Table -->
<section class="section">
    <div class="row justify-content-end ">
        <div class="col-2 text-end">
            <a class="btn btn-primary" asp-controller="AddRemoveRoles" asp-action="Create">Create</a>
        </div>

    </div>

    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">

                    <!-- Table with stripped rows -->
                    <table class="table datatable  table-striped  ">

                        <thead>
                            <tr>
                                <th scope="col">Role Name</th>
                                <th scope="col">User Name</th>
                                <th scope="col">DETAILS</th>
                                <th scope="col">Delete</th>

                            </tr>
                        </thead>

                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.RoleName</td>
                                    <td>@item.UserNAME</td>

                                    <td><a asp-controller="AddRemoveRoles" asp-action="details" asp-route-RoleID="@item.RoleID" asp-route-UserId="@item.UserId" class="btn btn-info">Details</a></td>
                                    <td>
                                        <a asp-controller="AddRemoveRoles" asp-action="Delete" asp-route-RoleID="@item.RoleID" asp-route-UserId="@item.UserId"
                                           class="btn btn-danger">Delete</a>
                                    </td>

                                </tr>
                            }

                        </tbody>
                    </table>
                    <!-- End Table with stripped rows -->
                </div>
            </div>
        </div>
    </div>
</section>
<!-- #END# Exportable Table -->
<!-- Jquery DataTable Plugin Js -->
 
@section DataTabelScripts {
       


}
@section PageTitle {
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="home" asp-action="index">Home</a></li>
        <li class="breadcrumb-item"> <a asp-controller="Roles" asp-action="index">Roles </a></li>
    </ol>
}




